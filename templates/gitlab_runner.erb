#!/bin/sh
#
# This file is managed via Puppet

daemon="/usr/local/bin/gitlab-runner"
daemon_flags="--log-level 'info' run"
daemon_user="<%= @user %>"

. /etc/rc.d/rc.subr

pexp="$daemon $daemon_flags.*"
rc_reload=NO
rc_bg=YES

rc_cmd $1
